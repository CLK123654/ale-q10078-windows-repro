检查项：交付路径与浏览器入口
标准答案：playwright.config.ts、tests/support_console_realtime.spec.ts、reports/case_results.csv、reports/socket_event_audit.csv、reports/focus_path.csv和artifacts/agent-billing-after-feed.png均存在；npm run test:e2e启动Playwright与Chromium并返回0。
关键字段：六个相对路径、Chromium项目、webServer、testDir。
容差：路径和扩展名不设容差；配置细节可以等价实现。
失败边界：任一文件缺失或不可读，测试未启动浏览器，或入口返回非0。
可观察证据：文件清单、Playwright日志、浏览器项目名和退出码。
客观硬性标准：六个交付物可读且五个场景均由Chromium执行。
阈值：全部满足。
优先级：Gate项
所属层级：L1任务指令遵循层

检查项：角色权限与REST路由
标准答案：frontline-agent只能看到agent-a或未分配且属于授权队列的会话；queue-manager可以跨坐席查看。CASE_AGENT_MINE_BILLING的有序集合为C1011、C1008、C1030、C1022，C1016不可见；CASE_MANAGER_PRIORITY为C1020、C1014、C1011。
关键字段：fixtures/agent_roles.json、fixtures/conversations.json、cases/realtime_cases.csv、reports/case_results.csv。
容差：集合成员和顺序不设容差，来源为角色字段、场景筛选及排序合同。
失败边界：直接把expected_visible_ids作为路由响应、改写fixture、泄露agent-b会话或遗漏主管跨队列会话。
可观察证据：page.route处理逻辑、页面data-conversation-id顺序和案例结果行。
客观硬性标准：两个指定场景的可见集合、顺序和隐藏边界全部正确。
阈值：全部满足。
优先级：高权重项
所属层级：L5多源关联与字段一致性层

检查项：Socket顺序回放与页面状态
标准答案：S01使C1022的unread从1变为3，S02使C1014的priority从P2变为P1，S03使C1030的assignee从unassigned变为agent-a，S04后C1020仍为单张卡片且status为escalated。
关键字段：fixtures/socket_feed.json、reports/socket_event_audit.csv、页面会话卡片。
容差：事件顺序、字段前值和后值不设容差。
失败边界：WebSocket在页面启动后才替换、漏发或调换事件、只改报告未触发message，或S04产生重复卡片。
可观察证据：page.addInitScript、message事件、body的data-last-event-id和四行事件审计。
客观硬性标准：S01至S04依次到达，四个字段变化与页面一致。
阈值：全部满足。
优先级：高权重项
所属层级：L2软件或工具操作层

检查项：会话排序与队列范围
标准答案：会话按P1、P2、P3，再按first_response_due_utc和conversation_id升序排列。technical队列的有序集合为C1014、C1027，billing队列在S03后为C1011、C1008、C1030、C1022。
关键字段：rules/console_contract.json的sort_order、会话priority、first_response_due_utc和conversation_id。
容差：排序键和结果顺序不设容差。
失败边界：依赖DOM原始位置、遗漏任一排序键、队列切换未过滤，或事件后没有重新排序。
可观察证据：页面data-conversation-id顺序、CASE_AGENT_MINE_BILLING与CASE_SCROLL_TECHNICAL结果。
客观硬性标准：两个队列的可见集合及顺序逐项一致。
阈值：全部满足。
优先级：高权重项
所属层级：L6领域计算与专业规则层

检查项：详情与固定时刻SLA
标准答案：CASE_SCROLL_TECHNICAL通过稳定定位打开C1027，详情区data-open-conversation为C1027且data-transcript-count为5；固定时刻2026-06-18T01:30:00Z下，C1008显示逾期8分钟，C1022显示4分钟后到期。
关键字段：fixtures/conversations.json、rules/console_contract.json、reports/case_results.csv。
容差：会话主键、条数和分钟值不设容差；显示标点可不同。
失败边界：使用屏幕坐标或nth定位、打开错误会话、读取本机当前时间或分钟方向错误。
可观察证据：详情区属性、SLA元素文字和两个案例结果行。
客观硬性标准：C1027详情与两条SLA文案均正确。
阈值：全部满足。
优先级：高权重项
所属层级：L8运行验证或可执行行为层

检查项：键盘焦点路径
标准答案：真实Tab路径依次到达global-search、queue-billing、conversation-C1011、reply-editor和send-button；在C1011按Enter打开详情，空回复框按Enter不改变当前会话。
关键字段：cases/realtime_cases.csv的focus_path、reports/focus_path.csv、活动元素。
容差：locator可以使用等价的稳定选择器，五步顺序不设容差。
失败边界：调用focus代替键盘路径、隐藏控件进入顺序、漏项、次序错误或空回复触发副作用。
可观察证据：keyboard.press调用、toBeFocused断言、五行焦点报告和详情主键。
客观硬性标准：五步焦点顺序与输入合同一致，键盘行为没有误触发。
阈值：全部满足。
优先级：高权重项
所属层级：L8运行验证或可执行行为层

检查项：报告与截图一致性
标准答案：case_results.csv含5行，socket_event_audit.csv含4行，focus_path.csv含5行；截图可打开并显示S01至S03后的billing队列，页面顺序为C1011、C1008、C1030、C1022。
关键字段：case_id、event_id、order_index、data-conversation-id和PNG文件。
容差：CSV允许LF或CRLF及必要引号；截图像素可因Chromium补丁版本有轻微差异。
失败边界：报告缺行或多行、主键无法关联、截图损坏，或截图内容与报告集合不同。
可观察证据：三份CSV、PNG尺寸与页面中的四个会话主键。
客观硬性标准：行数、主键、观察值和截图内容互相一致。
阈值：全部满足。
优先级：普通项
所属层级：L9跨产物一致性与可追溯层

检查项：输入保全与本地访问边界
标准答案：测试只读取input_data中的业务材料，正式运行只访问127.0.0.1及被page.route接管的/api/conversations，不修改fixtures、cases或rules文件。
关键字段：输入文件哈希、页面请求地址、测试源码中的文件写入路径。
容差：本机回环端口可以变化；输入内容不设容差。
失败边界：向外部地址发请求、写回业务输入、连接真实WebSocket或把预生成报告当作浏览器结果。
可观察证据：运行前后输入哈希、请求日志和写文件目标路径。
客观硬性标准：业务输入逐字节不变，外部网络请求数为0。
阈值：全部满足。
优先级：普通项
所属层级：L3输入数据导入与完整性层
